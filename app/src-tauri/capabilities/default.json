{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main Spork window: the renderer reaches the daemon only through the app's own #[tauri::command]s (open_project / dispatch / graph_view / set_agent_config), plus the native folder picker (dialog) used to select a project to onboard — never any other ambient FS/provider access (DESIGN.md §15.1).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "dialog:default"
  ]
}
