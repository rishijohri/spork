# Spork
Initial First Commit

## Run Instruction

cd app && npm install && npm run tauri:dev      # or VS Code → "Tauri Development Debug"
cd app && ./scripts/check.sh                     # re-run the full no-display green bar anytime
